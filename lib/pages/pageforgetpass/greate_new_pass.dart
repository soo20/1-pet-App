import 'package:flutter/material.dart';

class GreateNewPass extends StatelessWidget {
  const GreateNewPass({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}